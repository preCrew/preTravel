const photo = `flex relative w-[26vw] h-[26vw] bg-red rounded border border-gray5 flex justify-center items-center overflow-hidden`;
const file = `opacity-0 absolute w-full h-full z-[3]`;
const fileIcon = '';

const Input = {
  photo,
  file,
};

export default Input;
